import { smartAccountActions } from "permissionless";
import { signUserOperation } from "../../actions/index.js";
import { sponsorUserOperation } from "../../actions/paymaster/sponsorUserOperation.js";
export const zerodevPaymasterActions = (client) => ({
    sponsorUserOperation: async (args) => sponsorUserOperation(client, args)
});
export const kernelAccountClientActions = ({ sponsorUserOperation }) => (client) => ({
    ...smartAccountActions({ sponsorUserOperation })(client),
    signUserOperation: (args) => signUserOperation(client, {
        ...args,
        sponsorUserOperation
    })
});
//# sourceMappingURL=kernel.js.map