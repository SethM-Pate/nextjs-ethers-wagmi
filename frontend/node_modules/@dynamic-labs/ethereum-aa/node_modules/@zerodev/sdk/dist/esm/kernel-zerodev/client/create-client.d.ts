import { type PublicErc4337Client, type HttpTransport } from "@alchemy/aa-core";
import { type Chain } from "viem";
import type { PaymasterAndBundlerProviders } from "../paymaster/types.js";
interface ZeroDevClientConfig {
    chain: Chain;
    projectId: string;
    rpcUrl?: string;
    bundlerRpcUrl?: string;
    bundlerProvider?: PaymasterAndBundlerProviders;
}
export declare const createZeroDevPublicErc4337Client: ({ chain, rpcUrl, bundlerRpcUrl, projectId, bundlerProvider, }: ZeroDevClientConfig) => PublicErc4337Client<HttpTransport>;
export {};
