import { LocalAccount, PublicClient } from 'viem';
import { WalletUiUtils } from '@dynamic-labs/types';
type Props = {
    address: `0x${string}`;
    account: () => Promise<LocalAccount>;
    walletUiUtils: WalletUiUtils;
    provider: PublicClient;
};
export declare const createAccountWithUiConfirmation: ({ address, account, provider, walletUiUtils, }: Props) => LocalAccount;
export {};
