import { Chain, PublicClient, TransactionRequest, Transport } from 'viem';
export declare const estimateL1Fee: (publicClient: PublicClient<Transport, Chain>, transaction: TransactionRequest) => Promise<bigint>;
export declare const opStackSupportedChains: number[];
