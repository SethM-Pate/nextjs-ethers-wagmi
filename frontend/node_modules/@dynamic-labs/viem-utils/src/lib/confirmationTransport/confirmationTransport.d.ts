import { PublicClient } from 'viem';
import { WalletUiUtils } from '@dynamic-labs/types';
import { type InterceptTransportProps } from '../interceptTransport';
type ConfirmationTransportProps = {
    provider: PublicClient;
    walletUiUtils: WalletUiUtils;
} & InterceptTransportProps;
export declare const confirmationTransport: ({ transport, walletUiUtils, getAccounts, onPersonalSign, onSendTransaction, onSignTypedData, provider, }: ConfirmationTransportProps) => import("viem").CustomTransport;
export {};
