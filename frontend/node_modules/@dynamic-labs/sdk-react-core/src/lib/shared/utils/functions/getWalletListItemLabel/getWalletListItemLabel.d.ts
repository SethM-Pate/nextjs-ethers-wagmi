import { WalletOption } from '../../../types';
type Args = {
    wallet: WalletOption;
};
type Label = 'Last used' | 'Installed' | 'Multichain' | undefined;
export declare const getWalletListItemLabel: ({ wallet }: Args) => Label;
export {};
