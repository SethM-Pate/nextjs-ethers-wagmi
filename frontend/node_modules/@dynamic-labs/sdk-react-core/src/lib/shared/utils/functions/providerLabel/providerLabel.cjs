'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var sdkApi = require('@dynamic-labs/sdk-api');
var capitalize = require('../capitalize/capitalize.cjs');

const providerLabel = (provider) => {
    if (!provider) {
        return undefined;
    }
    if (provider === sdkApi.SocialSignInProviderEnum.Coinbasesocial) {
        return 'Coinbase';
    }
    return capitalize.capitalize(provider);
};

exports.providerLabel = providerLabel;
