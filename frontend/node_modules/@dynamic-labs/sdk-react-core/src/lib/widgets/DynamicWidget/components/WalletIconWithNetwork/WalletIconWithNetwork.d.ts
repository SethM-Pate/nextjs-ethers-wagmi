/// <reference types="react" />
import { Icon } from '../../../../shared';
type Props = {
    iconSize?: number;
    chainName: string;
    Icon: Icon;
    showNetwork?: boolean;
};
export declare const WalletIconWithNetwork: ({ iconSize, chainName, Icon, showNetwork, }: Props) => JSX.Element;
export {};
