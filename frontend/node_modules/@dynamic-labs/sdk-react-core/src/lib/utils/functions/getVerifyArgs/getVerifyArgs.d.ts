import { FetchPublicAddressOpts } from '@dynamic-labs/wallet-connector-core';
import { WalletAdditionalAddress } from '@dynamic-labs/sdk-api';
import { WalletConnector } from '../../../..';
import { WalletProvider } from '../../../shared';
type Props = {
    displaySiweStatement: boolean;
    environmentId: string;
    fetchPublicAddressOpts?: FetchPublicAddressOpts;
    nonce: string;
    publicWalletAddress: string;
    siweStatement: string;
    skipEmptyAccountCheck?: boolean;
    walletConnector: WalletConnector;
    walletProvider: WalletProvider;
    signedMessageOverride?: string;
    messageToSignOverride?: string;
};
type VerifyArgs = {
    additionalWalletAddresses: WalletAdditionalAddress[] | undefined;
    chain: string;
    messageToSign: string;
    network: string;
    publicWalletAddress: string;
    signedMessage: string;
    skipEmptyAccountCheck?: boolean;
    walletName: string;
    walletProvider: WalletProvider;
};
export declare const getVerifyArgs: ({ walletConnector, nonce, walletProvider, environmentId, displaySiweStatement, skipEmptyAccountCheck, siweStatement, publicWalletAddress, signedMessageOverride, messageToSignOverride, }: Props) => Promise<VerifyArgs>;
export {};
