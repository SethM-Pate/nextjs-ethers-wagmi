'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var decodeJwt = require('../../../shared/utils/functions/decodeJwt/decodeJwt.cjs');
require('@dynamic-labs/iconic');
require('@dynamic-labs/wallet-connector-core');
require('react/jsx-runtime');
require('../../../context/ViewContext/ViewContext.cjs');
require('react');
require('../../../shared/logger.cjs');
require('@dynamic-labs/wallet-book');
require('../../constants/colors.cjs');
var values = require('../../constants/values.cjs');
require('../../../shared/utils/classes/storage/localStorage.cjs');
require('viem');
require('@dynamic-labs/sdk-api');
require('@dynamic-labs/utils');
require('../../../shared/consts/index.cjs');

// TODO: Check if this is actually returning the correct verified credential
const findEmbeddedWalletFromVerifiedCredentials = (jwt, chain) => {
    var _a;
    const decodedJwt = decodeJwt.decodeJwt(jwt);
    const verifiedCredentials = (_a = decodedJwt === null || decodedJwt === void 0 ? void 0 : decodedJwt.verifiedCredentials) === null || _a === void 0 ? void 0 : _a.filter(({ walletProvider }) => walletProvider === 'embeddedWallet');
    if ((verifiedCredentials === null || verifiedCredentials === void 0 ? void 0 : verifiedCredentials.length) && chain) {
        const chainName = values.ChainEnumToVerifiedCredentialName[chain];
        return verifiedCredentials.find(({ chain }) => chain === chainName);
    }
    return verifiedCredentials === null || verifiedCredentials === void 0 ? void 0 : verifiedCredentials.shift();
};

exports.findEmbeddedWalletFromVerifiedCredentials = findEmbeddedWalletFromVerifiedCredentials;
