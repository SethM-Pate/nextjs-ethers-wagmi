import { DynamicJwt } from '@dynamic-labs/sdk-api';
type CredentialType = 'email' | 'phone' | 'wallet';
/**
 * This will return a string which we can present to the user credential type
 * @param decodedJwt
 * @returns CredentialType | undefined
 */
export declare const getUserVerifiedCredentialType: (decodedJwt: DynamicJwt) => CredentialType | undefined;
export {};
