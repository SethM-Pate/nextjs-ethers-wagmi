import { PublicClient, TransactionRequest } from 'viem';
/**
 * The interval to refresh the gas price in milliseconds.
 */
export declare const REFRESH_INTERVAL = 20000;
/**
 * Estimates the L1 fee for the transaction. if the chain is supported.
 */
export declare const useEstimateL1Fee: (publicClient: PublicClient, transaction: TransactionRequest) => import("../usePromise").PromiseState<bigint, Error>;
