import { DynamicJwt, ChainEnum } from '@dynamic-labs/sdk-api';
import { Wallet } from '../../../../shared';
export declare const useTurnkey: () => {
    readonly createTurnkeyWallet: (jwt: string, decodedJwt: DynamicJwt, chain?: ChainEnum) => Promise<Wallet | undefined>;
};
