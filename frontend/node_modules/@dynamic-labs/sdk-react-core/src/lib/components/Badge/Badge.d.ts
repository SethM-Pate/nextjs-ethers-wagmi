/// <reference types="react" />
import { CopyKey } from '../../shared';
import { BadgeProps } from './Badge.types';
export declare const Badge: ({ className, dot, text, copykey, }: BadgeProps & CopyKey) => JSX.Element;
