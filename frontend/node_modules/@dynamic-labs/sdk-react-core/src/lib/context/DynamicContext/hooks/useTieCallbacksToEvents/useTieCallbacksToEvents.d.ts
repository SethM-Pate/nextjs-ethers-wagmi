import { Dispatch, SetStateAction } from 'react';
import { DynamicEventsCallbacks, UserProfile, Wallet } from '../../../../shared';
export type UseTieCallbacksToEventsParams = {
    clientCallbacks: DynamicEventsCallbacks | undefined;
    connectedWallets: Wallet[];
    user: UserProfile | undefined;
    setHasLoggedOut: Dispatch<SetStateAction<boolean>>;
};
/** Ties the customer callbacks to our events, such that our events will also trigger the callbacks */
export declare const useTieCallbacksToEvents: ({ clientCallbacks, connectedWallets, user, setHasLoggedOut, }: UseTieCallbacksToEventsParams) => void;
